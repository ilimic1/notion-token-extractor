{
  "name": "@ilimic/notion-token-extractor",
  "version": "0.3.1",
  "description": "Extracts Notion tokens needed to make internal API calls.",
  "main": "build/index.js",
  "bin": "bin/notion-token-extractor",
  "scripts": {
    "test": "npm run build-tests && jest",
    "build-tests": "rm -rf build && rm -rf build-tests && tsc --build tests",
    "build": "rm -rf build && tsc --build src",
    "watch": "rm -rf build && cd src && tsc -w"
  },
  "author": "Ivo Limić",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "puppeteer": "^21.1.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/node": "^20.5.6",
    "jest": "^29.6.4",
    "typescript": "^5.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ilimic1/notion-token-extractor"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
