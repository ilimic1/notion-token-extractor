{
  "name": "@ilimic/notion-token-extractor",
  "version": "0.1.6",
  "description": "Extracts tokens from Notion needed to make internal API calls.",
  "main": "build/index.js",
  "bin": "bin/notion-token-extractor",
  "scripts": {
    "test": "jest",
    "build": "rm -rf build && tsc",
    "watch": "rm -rf build && tsc -w"
  },
  "author": "Ivo Limić",
  "license": "MIT",
  "dependencies": {
    "puppeteer": "^21.1.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/node": "^20.5.6",
    "jest": "^29.6.4",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ilimic1/notion-token-extractor"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
